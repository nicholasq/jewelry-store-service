micronaut:
  application:
    name: jewelryStoreService
netty:
  default:
    allocator:
      max-order: 3
firestore:
  contact:
    collection: 'contact'
  pipeline:
    collection: 'pipeline'
contact:
  collection: 'contact'
gcloud:
  project-id: ${GCLOUD_PROJECT_ID:`test-project`}
